# Лабораторна робота №3
## Робота з файлами, логування та обробка виключень

**Максимальна оцінка — 9 балів.**

### 🎯 Що ви навчитеся робити

Структурувати дані за допомогою `enum` і `record`, керувати потоком виконання через `switch`-вирази — і бачити, який саме код при цьому зникає.

---

### 📦 Де працюєте

Репозиторій той самий, що в ЛР1 — домен і варіант теж. Профіль вашого варіанта: **[Доменні профілі](../lab-01/tasks_2025_domains.md)**.

Роботу робите **в окремій гілці `lab2`**, здаєте через pull request у `main`:

```
git checkout -b lab2
... ваш код ...
git push -u origin lab2
```

Далі на GitHub відкриваєте pull request `lab2` → `main`. Захист проходить по діффу у вашому PR — там має бути видно, що саме змінилося порівняно з ЛР1. Після захисту PR мерджиться.

Перше, з чим ви зіткнетесь: **`record` не може успадковувати клас**, тобто `BaseEntity` із ЛР1 доведеться прибрати. Це не помилка в завданні — це межа наслідування. Що робити з `createdAt`, вирішуєте самі та обґрунтовуєте на захисті.

---

### 📋 Що зробити

**1. Замініть поля-переліки на `enum`.** Усе, що в ЛР1 було `String` із фіксованим списком значень (`status`, `type`, `category`, `zone`…), стає окремим `enum` у пакеті `ua.<домен>.model` — з тими самими константами, що в профілі.

Щонайменше один `enum` має власне поле і конструктор:

```java
public enum RoomType {
    SINGLE(1.0), DOUBLE(1.6), SUITE(2.5), DELUXE(3.2);

    private final double priceFactor;

    RoomType(double priceFactor) {
        this.priceFactor = priceFactor;
    }

    public double getPriceFactor() {
        return priceFactor;
    }
}
```

Перевірка «чи значення є в списку допустимих» після цього стає непотрібною — чому саме, спитаю на захисті.

**2. Переробіть у `record` класи, поля яких не повинні змінюватися після створення.** Класи, стан яких змінюється, лишаються класами — у профілі позначено, які саме поля змінювані.

Валідація переїжджає в компактний конструктор:

```java
public record Room(int roomNumber, RoomType type, double pricePerNight) {
    public Room {
        HotelUtils.requireInRange(roomNumber, 1, 999, "roomNumber");
        HotelUtils.requirePositive(pricePerNight, "pricePerNight");
    }
}
```

Пакет `util` лишається як був — і `ValidationHelper`, і `FormatHelper`, і `<Домен>Utils`.

**3. Видаліть із `record`-ів усе, що тепер генерує компілятор:** геттери, `equals`, `hashCode`, `toString`.

**4. Напишіть два `switch`-вирази** — саме вирази (Java 14+), зі стрілками і поверненням значення, а не `switch`-інструкції. Обидва живуть у `<Домен>Utils`, поруч із обчислюваними методами з ЛР1:

- перший — по вашому `enum`, **без `default`**: перелічіть усі константи;
- другий — там, де в домені є вибір поведінки: коефіцієнт ціни, доступна дія за статусом, категорія знижки, текст для користувача.

**5. Оновіть `Main`.** Показати треба:

1. створення `record`-обʼєкта і звернення до його компонентів;
2. `enum` у роботі: перебір `values()`, доступ до власного поля константи, `valueOf` на неправильному рядку в `try/catch`;
3. обидва ваші `switch`-вирази;
4. `record`, створений з невалідними даними → `IllegalArgumentException` із компактного конструктора;
5. два однакові `record`-обʼєкти: `equals` і `hashCode` — без жодного рядка, написаного вами;
6. вивід `record` через згенерований `toString()`.

---

### ✍️ Оформлення

Коментарі, javadoc і тексти винятків — англійською. Українською — тільки те, що бачить користувач: рядки виводу в `Main`.

---

### 🤖 Частина про AI

5–7 речень:
- чим користувалися і що просили — ключовий промпт наведіть дослівно;
- **що AI зробив неправильно або не так, як вимагає завдання**, і як ви це помітили;
- що виправили самі й чому.

Якщо не користувалися — покажіть AI одне своє рішення і опишіть, з чим у його відповіді погоджуєтесь, з чим ні й чому.

---

### 🎤 Захист

5 хвилин, 3–4 питання по **вашому** коду й по діффу у вашому PR. Орієнтовно:

- Чому `record` не може успадкувати `BaseEntity`? Що ви зробили з `createdAt` і чому саме так?
- Які класи ви залишили класами і за яким правилом?
- Що саме компілятор згенерував за вас у `record`? Як виглядає його `equals`?
- Чому в компактному конструкторі не пишуть `this.field = field`?
- Чому `switch`-вираз по `enum` можна писати без `default`, а по `String` — ні? Що станеться, якщо завтра в `enum` додати константу?
- Чим `switch`-вираз відрізняється від `switch`-інструкції? Що таке провалювання (fall-through) і чому його тут немає?
- Куди поділася перевірка «значення є в списку допустимих» після переходу на `enum`?
- Чи є у вашому `record` поле-масив або змінюваний обʼєкт? Наскільки він тоді справді незмінний?

---

### 📊 Оцінювання

| Складова | Бали |
|---|---|
| `enum`-и з константами профілю, один — із власним полем і конструктором | 2 |
| Перехід на `record`: правильно обрані класи, валідація в компактному конструкторі | 2 |
| Видалено все, що тепер генерує компілятор | 1 |
| Два `switch`-вирази, один — вичерпний по `enum` без `default` | 2 |
| Клас `Main`: усі сценарії | 1 |
| Захист | 1 |
| **Разом** | **9** |

Робота без частини про AI не приймається.

Мета: Навчитися обробляти виключення в Java, реалізувати власний клас виключень, працювати з файлами та рядками, а також логувати події програми.
Завдання:
Створити власний клас виключень InvalidDataException для обробки некоректних даних.
Дані для  незалежних класів (без агрегації) зчитувати з файлу. Формат файлу – довільний (наприклад, CSV або текстові рядки).
Реалізувати обробку виключень для:
некоректних даних при зчитуванні з файлу;
потенційних помилок при роботі з файлами (FileNotFoundException, IOException);
будь-яких інших логічних помилок у класах.
Використати блоки try-catch-finally та, за потреби, multi-catch.
Додати логування усіх критичних та інформаційних подій програми (створення об’єктів, обробка виключень, успішне завершення операцій).
Написати базові тести для перевірки:
коректного створення об’єктів,
обробки некоректних даних (InvalidDataException),
правильності логіки методів класів.
Створити демонстраційний клас Main, який:
зчитує дані з файлу;
створює об’єкти  класів;
обробляє можливі помилки через власні та стандартні виключення;
демонструє роботу логування.
